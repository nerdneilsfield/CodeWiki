# Code Review — 2026-03-05 01:41:33 (cbe4ed)

## Scope

- Uncommitted changes: none (`git diff`, `git diff --cached` were empty).
- Recent commits reviewed (`git log --oneline -5`):
  - `c26f813` fix(test): tighten Retry-After negative-value assertion; share _MAX_RETRY_AFTER constant
  - `f3a3166` fix(llm): clamp Retry-After header to [0, 120s] to prevent crash or blocking
  - `f829342` perf(orchestrator): thread pre-built models through CodeWikiDeps to sub-agent tool
  - `e0405db` perf(orchestrator): pre-build model objects in __init__, reuse in create_agent
  - `a06a1b4` perf(llm): cache AsyncOpenAI and OpenAIProvider via lru_cache
- Agents run: Diff, Holistic, UX (UX skipped: no running dev server reachable on common localhost ports).

## Findings (Deduplicated)

### Critical

1) Missing license file
- Location: `LICENSE` (missing)
- Severity: critical
- Category: Holistic
- Description: `README.md` references `./LICENSE` and `pyproject.toml` declares MIT, but there is no `LICENSE` file checked in. This creates legal/redistribution ambiguity for users and downstream packagers.
- Suggested fix: Add a `LICENSE` file (MIT text) and ensure it is included in source distributions/wheels.

### High

1) Placeholder repository URLs in package metadata
- Location: `pyproject.toml:80`
- Severity: high
- Category: Holistic
- Description: `[project.urls]` points to `https://github.com/yourusername/codewiki` (placeholder), which will mislead users and break “Homepage/Issues/Docs” links on package indexes.
- Suggested fix: Replace with canonical repo/docs/issues URLs and ensure `README.md` links/badges match.

2) Conflicting dependency sources-of-truth
- Location: `requirements.txt`, `pyproject.toml`
- Severity: high
- Category: Holistic
- Description: `requirements.txt` appears to be a large transitive “freeze” while `pyproject.toml` declares a smaller direct dependency set. This makes installs non-reproducible across environments and risks “works in Docker, fails in pip install” drift.
- Suggested fix: Choose one source-of-truth (prefer `pyproject.toml` plus a lockfile like `uv.lock`/`poetry.lock`, or `requirements.in` → compiled lock), and align Docker/CI installs to it.

3) Default generated-doc output path collides with repo documentation
- Location: `codewiki/cli/models/config.py:128`
- Severity: high
- Category: Holistic
- Description: Default output is `docs`, but the repo already uses `docs/` for project documentation (e.g., plans). This risks overwriting tracked docs and makes “repo docs” vs “generated docs” ambiguous for humans and agents.
- Suggested fix: Change default to something like `output/docs` or `generated-docs/`, and update `README.md` / `DEVELOPMENT.md` accordingly.

4) Agent harness hygiene: local `.claude/` artifacts present; no repo-wide agent rules
- Location: `.claude/`, repo root (missing `AGENTS.md` / `CLAUDE.md`)
- Severity: high
- Category: Holistic
- Description: Machine-specific artifacts exist in `.claude/` (e.g., `settings.local.json`, `review-loop.log`), but there is no repo-level `AGENTS.md` guidance for agents (conventions, testing commands, output paths, safety notes), and `.claude/` is not clearly treated as local-only.
- Suggested fix: Add a repo-root `AGENTS.md` (and optionally `CLAUDE.md` compatibility link/file) and either gitignore `.claude/` or replace with sanitized committed templates (e.g., `.claude/settings.example.json`).

5) No CI enforcement detected
- Location: repo root (no `.github/workflows/` directory)
- Severity: high
- Category: Holistic
- Description: No CI configuration detected, so formatting/lint/type checks/tests are not enforced on changes.
- Suggested fix: Add minimal CI to run `ruff`, `mypy`, and `pytest` on PRs; keep it aligned with the dependency source-of-truth.

6) `.env` example format likely breaks Docker Compose `env_file`
- Location: `docker/env.example:7`
- Severity: high
- Category: Holistic
- Description: `docker/env.example` uses `KEY = value` (spaces around `=`), which commonly breaks Compose `env_file` parsing even if `python-dotenv` tolerates it.
- Suggested fix: Normalize to strict `KEY=value` in all `.env` examples and docs; ensure `docker/docker-compose.yml` references the intended env file.

### Medium

1) Cached async LLM clients/providers have no explicit teardown
- Location: `codewiki/src/be/llm_services.py:27`
- Severity: medium
- Category: Diff
- Description: `@lru_cache` now keeps `AsyncOpenAI` / `OpenAIProvider` instances alive for the process lifetime, but there is no explicit shutdown/close path. In long-running processes this can leak resources and complicate reuse across event loops/threads.
- Suggested fix: Add a teardown API that closes underlying clients and calls `cache_clear()` (e.g., on FastAPI lifespan shutdown / CLI exit).

2) Potential behavior change: default model selection bypassed for non-long-context path
- Location: `codewiki/src/be/agent_orchestrator.py:98`
- Severity: medium
- Category: Diff
- Description: `create_agent()` no longer calls `select_agent_model()` for the default path and instead always uses `self.fallback_models`. If `select_agent_model()` previously chose a different “normal/default” model, this changes behavior for small modules.
- Suggested fix: Either preserve the prior selection semantics (while still reusing prebuilt models/providers), or make the intent explicit and add a test asserting model choice for typical token sizes.

3) “Perf” tests may execute real factory implementations via `wraps=...`
- Location: `tests/test_perf_async_client_pool.py:70`
- Severity: medium
- Category: Diff
- Description: The tests patch `create_fallback_models` / `create_long_context_model` with `wraps=...`, meaning real implementations still run. If those build real clients or do non-trivial work, tests can become slow/flaky and less isolated.
- Suggested fix: Patch factories with lightweight `return_value` sentinels and/or patch lower-level client/provider constructors to ensure no real network/IO-capable clients are created.

4) Pre-commit hooks not configured
- Location: repo root (missing `.pre-commit-config.yaml`)
- Severity: medium
- Category: Holistic
- Description: Tooling appears configured in `pyproject.toml`, but there is no pre-commit configuration to keep formatting/lint issues from landing.
- Suggested fix: Add `.pre-commit-config.yaml` (e.g., ruff + formatting + basic hygiene) and document `pre-commit install`.

5) Docker build may diverge from packaged install
- Location: `docker/Dockerfile`, `docker/docker-compose.yml`
- Severity: medium
- Category: Holistic
- Description: Docker appears to install from `requirements.txt` rather than building/installing the package, which can diverge from `pip install codewiki` behavior.
- Suggested fix: Prefer `pip install .` (or `pip install -e .` for dev images) and use a lock/constraints file consistent with `pyproject.toml`.

6) Design docs directory is gitignored
- Location: `.gitignore:75` (ignores `codewiki-docs/`)
- Severity: medium
- Category: Holistic
- Description: `codewiki-docs/` exists but is ignored, making the “source of truth” for design/architecture unclear and easy to lose across machines.
- Suggested fix: Either track these docs under `docs/` (e.g., `docs/design/`) or treat them as generated output and relocate under `output/`.

7) Repo contains build/runtime artifacts in-tree
- Location: repo root (e.g., `codewiki.egg-info/`, `__pycache__/`, `.DS_Store`, `output/`)
- Severity: medium
- Category: Holistic
- Description: Multiple generated artifacts are present in the working tree, which creates noise and can confuse agents about what is “source” vs “generated”.
- Suggested fix: Add a `clean` command/script and document expected generated-output locations; keep generated artifacts out of the repo by default.

8) Some tests likely not collected by default pytest config
- Location: `pyproject.toml:123`, `test_math.py`, `test_math2.py`, `test_format.py`
- Severity: medium
- Category: Holistic
- Description: Pytest `testpaths = ["tests"]` means root-level tests won’t run in normal workflows, which can cause false confidence.
- Suggested fix: Move these tests under `tests/` (or delete if they’re scratch files) so they run consistently.

9) Packaging config is manually enumerated (brittle)
- Location: `pyproject.toml:86`
- Severity: medium
- Category: Holistic
- Description: `[tool.setuptools].packages` is manually listed. It’s easy to forget to add new subpackages, leading to silently missing modules in distributions.
- Suggested fix: Switch to automatic discovery (e.g., `setuptools.find_packages`) with explicit include/exclude patterns.

10) Observability configuration appears partially present but not clearly supported
- Location: `codewiki/src/be/agent_orchestrator.py`, `requirements.txt`, `docker/env.example`
- Severity: medium
- Category: Holistic
- Description: Observability deps/env knobs exist (e.g., OpenTelemetry/Logfire signals), but instrumentation appears incomplete/commented, creating dead configuration surface area.
- Suggested fix: Either fully support and document observability (extras + enable flag + consistent initialization) or remove unused deps/config to reduce maintenance burden.

11) UX review skipped (no running dev server reachable)
- Location: n/a
- Severity: medium
- Category: UX
- Description: No already-running local dev server was reachable on `127.0.0.1` ports `3000`, `5173`, `8000`, or `8080` (connection refused), so browser-based UX checks were not performed.
- Suggested fix: Start the dev server and rerun UX review (including desktop/mobile viewport checks and accessibility pass).

### Low

1) Private constant import + duplicated Retry-After parsing logic
- Location: `codewiki/src/be/documentation_generator.py:22`
- Severity: low
- Category: Diff
- Description: Imports `_MAX_RETRY_AFTER` (private symbol) from `llm_services` and duplicates parsing/clamping logic even though `_parse_retry_after()` exists.
- Suggested fix: Promote a public constant (e.g., `MAX_RETRY_AFTER_SECONDS`) or move constants to a shared module; reuse the shared parser where appropriate.

2) Fallback model selection line is long; “prebuilt once” intent is not enforced
- Location: `codewiki/src/be/agent_tools/generate_sub_module_documentations.py:165`
- Severity: low
- Category: Diff
- Description: The fallback assignment line is very long and (if `ctx.deps.fallback_models` is unset) can still call `select_agent_model(...)`, potentially recreating models per call and undermining the “prebuild once” performance goal.
- Suggested fix: Wrap for readability; ensure `fallback_models` is always populated in deps (or make the legacy path explicit).

3) Minor docs drift and “single source of truth” ambiguity
- Location: `README.md`, `DEVELOPMENT.md`
- Severity: low
- Category: Holistic
- Description: Docs are strong overall, but some details drift (metadata URLs vs README links; generated-doc output directory ambiguity).
- Suggested fix: Add a short “Single Source of Truth” section (canonical repo URL, install path, default output dirs) and keep it aligned with `pyproject.toml`.

## Strengths (Non-Blocking)

- `README.md` and `DEVELOPMENT.md` are unusually comprehensive and make the CLI/BE/FE split discoverable.
- `pyproject.toml` centralizes tool configuration (`ruff`/`mypy`/`pytest`) and signals typing (`py.typed`).
- The recent LLM retry-after clamping + jitter handling is a good stability improvement; performance-oriented model reuse changes are directionally sound (with the caveats above).

## Summary

- Total issues: 21
  - Critical: 1
  - High: 6
  - Medium: 11
  - Low: 3
- Agents:
  - Diff: ran (changed-code review for last 5 commits; no uncommitted changes)
  - Holistic: ran (structure/maintainability/agent readiness)
  - UX: skipped (no running dev server detected)
- Overall assessment: Solid recent work on retry-after robustness and model reuse, but repo hygiene/agent-readiness needs attention (license, CI, dependency source-of-truth, output paths, and local tooling artifacts) to make future changes safer and easier to automate.

