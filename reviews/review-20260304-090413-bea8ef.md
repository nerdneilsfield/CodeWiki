# Consolidated Review

Date: 2026-03-04  
Repository: `/home/dengqi/Source/langs/python/CodeWiki`  
Output: `reviews/review-20260304-090413-bea8ef.md`

Agents run:
- Diff Review agent (completed)
- Holistic Review agent (completed)
- UX Review agent (completed; dev server reachable at `http://127.0.0.1:8002`)

Deduplication notes:
- Merged overlapping findings about root `test_*.py` scripts into a single medium-severity issue focused on missing executable pytest coverage.

## Critical

| File path / directory | Line | Severity | Category | Description | Suggested fix |
|---|---:|---|---|---|---|
| `pyproject.toml` | N/A | critical | Holistic | Runtime modules import packages that are not declared in `[project.dependencies]` (e.g., `fastapi`/`uvicorn` in FE modules and `tree_sitter_*` language packages in analyzers), so `pip install .` can produce a partially broken runtime. | Make `pyproject.toml` the single source of runtime dependencies; add all required runtime packages there, keep dev-only packages separate, and add CI smoke imports for web app and analyzer modules. |

## High

| File path / directory | Line | Severity | Category | Description | Suggested fix |
|---|---:|---|---|---|---|
| `codewiki/src/be/agent_tools/str_replace_editor.py` | 459 | high | Diff | `validate_path()` enforces `path.resolve().is_relative_to(self.absolute_docs_path)` unconditionally, but `EditTool` is constructed with `absolute_docs_path` even in `working_dir="repo"` mode. This can reject repo-mode `view` operations outside docs and break expected behavior. | Make allowed base path depend on working mode (docs vs repo), or pass an explicit allowed base path into `validate_path()`. Add regression tests for both `working_dir="docs"` and `working_dir="repo"`. |
| `pyproject.toml`, `codewiki/__init__.py` | N/A | high | Holistic | Version metadata is inconsistent (`pyproject.toml` shows `2.0.0`, package `__version__` exports `1.0.1`), risking incorrect CLI/package reporting. | Use one source of truth for versioning (e.g., dynamic version from metadata or `setuptools_scm`) and remove duplicated hardcoded versions. |
| `README.md` | N/A | high | Holistic | README declares MIT license and links `./LICENSE`, but no license file is present in repository root. | Add the correct `LICENSE` file or update docs/metadata to match actual license status. |
| Route: `/Design_Tutorials-beamformer.html` | N/A | high | UX | Screenshot: `/tmp/codewiki-ux/mobile-Design_Tutorials-beamformer.html.png`. Internal navigation target returns `404`, causing dead-end navigation. | Generate the missing page or fix the link target; add automated broken-link checks in build/CI. |
| Invalid route handling | N/A | high | UX | Screenshots: `/tmp/codewiki-ux/desktop-404.png`, `/tmp/codewiki-ux/mobile-404.png`. 404 state is a blank gray page with no guidance or recovery action. | Serve a styled 404 page with clear message and CTA(s) such as “Back to Home”. |

## Medium

| File path / directory | Line | Severity | Category | Description | Suggested fix |
|---|---:|---|---|---|---|
| `test_math.py`, `test_math2.py`, `test_format.py` | 1 | medium | Diff | Newly added root `test_*.py` files are script-style print checks rather than pytest test functions; they are not collected by configured pytest discovery and do not provide automated regression coverage for math/mermaid behavior. | Convert these into real `pytest` tests with assertions under `tests/`, including cases for `_validate_math`, `_fix_math_in_text`, and Mermaid validation/repair branches. |
| `pyproject.toml` | N/A | medium | Holistic | `project.urls` still uses placeholder/fragmented repository identity (e.g., `yourusername/codewiki`), while docs reference different locations. | Define and use one canonical repository/docs/issues URL set across metadata and docs. |
| `docker/DOCKER_README.md`, `docker-compose.yml` | N/A | medium | Holistic | Docker documentation is out of sync with checked-in compose configuration (e.g., documented `PYTHONPATH` differs from compose setting), which increases onboarding errors. | Align Docker docs with actual compose files and add a lightweight docs consistency check in CI. |
| `codewiki/src/config.py` (and related CLI/backend config handling) | N/A | medium | Holistic | Configuration and instruction-generation logic is duplicated across components, increasing drift risk and maintenance cost. | Extract shared config/instruction schema and parsing utilities into a common module used by both backend and CLI paths. |
| `codewiki/src/be/` | N/A | medium | Holistic | Several core modules are very large (`prompt_template.py`, `guide_generator.py`, `documentation_generator.py`), suggesting mixed responsibilities and lower change isolation. | Split large modules by responsibility (prompt definitions, orchestration, IO/services) and enforce complexity/module-size checks. |
| `codewiki/run_web_app.py` | N/A | medium | Holistic | Web startup relies on `sys.path` mutation and a separate startup path from core package flows, creating fragile import/runtime behavior. | Move to a package-native entrypoint and unify bootstrap/config path across CLI and web execution. |
| Repository root (`/home/dengqi/Source/langs/python/CodeWiki`) | N/A | medium | Holistic | Agent/developer harness is incomplete: no in-repo `AGENTS.md`/`CLAUDE.md`, no `.github` workflows, and sparse/empty CLI+FE test areas for critical flows. | Add a minimal `AGENTS.md` runbook and CI workflows (lint, unit, smoke tests), and populate CLI/FE tests with meaningful coverage. |
| Mobile docs pages (e.g., `/guide-getting-started.html`, `/index.html`, `/polynomial_vectorization.html`) | N/A | medium | UX | Screenshots: `/tmp/codewiki-ux/mobile-guide-getting-started.html.png`, `/tmp/codewiki-ux/mobile-index.html.png`, `/tmp/codewiki-ux/mobile-polynomial_vectorization.html.png`. Horizontal overflow on 375px viewport causes right-side gutter and awkward scrolling. | Constrain overflow to specific content blocks (`pre`, tables) using `max-width: 100%` + `overflow-x: auto`; ensure long tokens wrap/scroll locally. |
| Syntax-highlighted code blocks | N/A | medium | UX | Accessibility scan reported `color-contrast` violations (serious impact), reducing readability. | Adjust syntax token colors in light/dark themes to meet WCAG AA contrast thresholds. |
| Scrollable code regions | N/A | medium | UX | Accessibility scan reported `scrollable-region-focusable` violations; keyboard users may be unable to focus/scroll horizontal code regions. | Add `tabindex="0"` and visible focus styles to scrollable regions; validate keyboard scrolling behavior. |

## Low

| File path / directory | Line | Severity | Category | Description | Suggested fix |
|---|---:|---|---|---|---|
| `DEVELOPMENT.md` | N/A | low | Holistic | Development structure docs reference directories that no longer exist (e.g., `paper/`), reducing trust in onboarding docs. | Refresh architecture/tree sections from the current repository state and keep them validated or generated during release. |
| Mobile sidebar drawer behavior | N/A | low | UX | Sidebar opened via keyboard is not dismissible with `Escape`, weakening expected keyboard interaction patterns. | Add `Escape` handler to close sidebar/overlay and restore focus to toggle control. |

## Summary

- Total issues: 18
- Severity breakdown: critical 1, high 5, medium 10, low 2
- Agents completed: Diff, Holistic, UX
- Overall assessment: The repository has one critical packaging/dependency integrity risk and several high-impact usability/documentation issues. Core architecture is workable but needs dependency hygiene, version/licensing consistency, stronger harness/CI guardrails, and targeted UX/accessibility hardening.
